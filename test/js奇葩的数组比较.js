/**
 * 数组的比较不能使用==，而应该使用既不小于也不大于
 *
 * 原因是==时不会把数组转成字符串，而小于和大于的时候会把对象转成字符串。
 * */
const a = [1, 2, 3]
const b = [1, 2, 3]
const c = [1, 2, 4]
console.log(`a<b=${a < b}
a>b=${a > b}
a==b=${a == b}
a==b=${!(a < b) && !(a > b)}
a<c=${a < c}
`)
