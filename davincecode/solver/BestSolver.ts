import {DeepSolver} from "./DeepSolver";
import {SimpleSolver} from "./SimpleSolver";

export const simpleSolver = new SimpleSolver();
export const deepSolver = new DeepSolver();
export const bestSolver = simpleSolver;
